//inherit the parent event
event_inherited();

//create a selected slot variable
selected_slot = noone;

x = 0;
y = sprite_get_height(spr_inventory_refinery)

depth = obj_inv_panel_toolbar.depth + 1;